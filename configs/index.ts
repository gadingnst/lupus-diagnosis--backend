// define root config here

export const PUBLIC_PATH = `${global.__basedir}/public`
export const STORAGE_PATH = `${global.__basedir}/storage`
export const DATASET_PATH = `${global.__basedir}/datasets`
